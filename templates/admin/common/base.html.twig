<!doctype html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport"
          content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <link rel="icon" type="image/png" href="{{ asset('uploads/admin/img/icon.png') }}">
    <link href="https://fonts.googleapis.com/css?family=Comfortaa|Quicksand|Raleway&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
    <link rel="stylesheet" type="text/css" href="{{ asset('dist/css/admin/style.css') }}">
    <title>Crèche Multi-Accueil LA CALINERIE - Espace ADMIN</title>
</head>

<body>

<header>
    <div class="dashboardDesk">
        <p>
            Tableau de Bord
        </p>
        <div class="logout">
            <a href="{{ path('app_login') }}">Se Déconnecter</a>
        </div>
    </div>
    <navResp>
        <div class="drop-down">
            <nav role='navigation'>
                <h1 class="textLogo">
                    <span id="t1">L</span>a Ca<span id="t2">l</span>ine<span id="t3">r</span>ie
                </h1>
                <ul>
                    <li><a href="{{ path('admin_dashboard') }}">ACCUEIL</a></li>
                    <li><a href="{{ path('admin_articles_list') }}">ARTICLES</a></li>
                    <li><a href="{{ path('admin_events_list') }}">ÉVÈNEMENTS</a></li>
                    <li><a href="{{ path('admin_pictures_list') }}">GALERIE</a></li>
                    <li><a href="{{ path('admin_menus_list') }}">MENUS</a></li>
                    <li class="mainDashboardCat"><a href="{{ path('admin_allergens_list') }}">ALLERGÈNES</a></li>
                    <li class="mainDashboardCat"><a href="{{ path('admin_children_list') }}">ENFANTS</a></li>
                    <li class="mainDashboardCat"><a href="{{ path('admin_users_list') }}">FAMILLES</a></li>
                    <li><a class="logoutResp" href="{{ path('app_login') }}">Se Déconnecter</a></li>
                </ul>
            </nav>
        </div>
        <div class="menuResp">
            <a class="trigger" href="#"><i class="fa fa-navicon"></i></a>
            <a class="close" href="#"><i class="fa fa-times"></i></a>
        </div>
    </navResp>
</header>

<aside>
    <div class="menuDesk">
        <nav>
            <div class="imgMenu">
                <a href="{{ path('admin_dashboard') }}">
                    <img src="{{ asset('uploads/admin/img/logo.png') }}" alt="Logo La Calinerie" class="img-fluid">
                </a>
            </div>
            <span>Admin, Bonjour !</span>
            <ul>
                <li class="select"><a href="{{ path('admin_articles_list') }}">ARTICLES</a></li>
                <li class="select"><a href="{{ path('admin_events_list') }}">ÉVÈNEMENTS</a></li>
                <li class="select"><a href="{{ path('admin_pictures_list') }}">GALERIE</a></li>
                <li class="select"><a href="{{ path('admin_menus_list') }}">MENUS</a></li>
            </ul>
        </nav>
    </div>
</aside>

{# j'affiche tous les messages flashs enregistrés #}
{% for message in app.flashes('success') %}
    <div class="container">
        <div class="flash">
            {{ message }}
        </div>
    </div>
{% endfor %}

{% block content %}
{% endblock %}

<script
        src="https://code.jquery.com/jquery-3.4.1.slim.min.js"
        integrity="sha256-pasqAKBDmFT4eHoN2ndd6lN370kFiGUFyTiUHWhU7k8="
        crossorigin="anonymous"></script>
<script src="{{ asset('src/js/app.js') }}"></script>

</body>
</html>
