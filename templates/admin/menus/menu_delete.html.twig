{% for message in app.flashes('success') %}
    <div class="message">
        {{ message }}
    </div>
    <div>
        <a href="{{ path ('admin_menus_list') }}"> - Revenir à l'ensemble des menus - </a>
    </div>
{% endfor %}