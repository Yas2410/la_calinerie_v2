<div>
    <h1>{{ event.title }}</h1>
    <h6>{{ event.date|date('d/m/Y') }}</h6>
    <div>
        <img src="{{ asset ('uploads/admin/events/') ~ event.image }}" alt="">
    </div>
    <p> {{ event.content }}</p>
    <div>
        <a href="{{ path ('admin_event_update', {'id':event.id}) }}"> - Modifier l'évènement - </a>
    </div>
    <div>
        <a href="{{ path ('admin_event_delete', {'id':event.id}) }}"> - Supprimer l'évènement - </a>
    </div>
    <div>
        <a href="{{ path ('admin_events_list') }}"> - Revenir à l'ensemble des évènements - </a>
    </div>
</div>