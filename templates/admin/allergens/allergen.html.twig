{% extends 'admin/common/base.html.twig' %}

{% block content %}

    <section class="containerInsert">
        <div class="singleCatInsert xLarge-3 large-3 medium-6 small-9">
            <div class="details">
                <div>
                    <h1>{{ allergen.type }}</h1>
                </div>
                <div class="list">
                    <h3> Liste des enfants concernés </h3>
                    {% for children in allergen.children %}
                    <p>
                        {{ children.firstName }}
                        {% endfor %}
                    </p>
                </div>
                <div class="action">
                    <a href="{{ path ('admin_allergen_update', {'id':allergen.id}) }}">
                        <i class="fa fa-edit"></i>
                    </a>
                    <a href="{{ path ('admin_allergen_delete', {'id':allergen.id}) }}">
                        <i class="fa fa-remove"></i>
                    </a>
                    <a href="{{ path ('admin_allergens_list') }}">
                        <i class="fa fa-reply"></i>
                    </a>
                </div>
            </div>
        </div>
    </section>

{% endblock %}

