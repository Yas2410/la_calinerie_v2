{% for message in app.flashes('success') %}
    <div class="message">
        {{ message }}
    </div>
    <div>
        <a href="{{ path ('admin_children_list') }}"> - Revenir à la liste des enfants - </a>
    </div>
{% endfor %}