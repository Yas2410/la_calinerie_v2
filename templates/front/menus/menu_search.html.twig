<div>
    <a href="{{ path ('home_parents') }}"> - Accueil - </a>
</div>
<h5>Vous avez recherché : {{ search }}</h5>

<div>
    {% for menu in menus %}
    <div>
        <h1>{{ menu.title }}</h1>
<p>{{ menu.content }}</p>
        <div>
            <a href="{{ path ('menu', {'id':menu.id}) }}"> - Details - </a>
        </div>
        {% endfor %}
    </div>
</div>
<div>
    <a href="{{ path ('menus') }}"> - Revenir à la liste des menus - </a>
</div>